.env
tiktok_auth.json
plan

output/
caption_output/
burn_output/
burn_uploads/
video-output/
projects/

frontend/node_modules/
frontend/dist/
frontend/playwright-report/
frontend/test-results/

__pycache__/
*.pyc
*.pyo

telegram_config.json

.DS_Store
.claude/
# …but shared project infra under .claude/ IS tracked (workflows, skills, and the
# asset-schema enforcement hook/settings — these must travel with the repo so every
# agent/machine gets the same guardrails, not just whoever set them up locally).
!.claude/
!.claude/workflows/
!.claude/workflows/**
!.claude/skills/
!.claude/skills/**
!.claude/hooks/
!.claude/hooks/**
!.claude/settings.json
# personal/local state stays ignored
.claude/settings.local.json
.claude/.DS_Store
.vercel

# Temporary font preview PNGs
font_previews/
test-assets/
