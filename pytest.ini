[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    real_route_auth: run the real per-route auth dependency (not the conftest bypass)
