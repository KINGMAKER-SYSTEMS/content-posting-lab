# S3 — The Fable 5 Story: Anthropic Ships the Enforcement Primitives | targetWords: 170±20 | keywords: Claude Code hooks, Claude Code subagents, agent teams, stop hook, Fable 5 Claude Code, task budgets

OpenAI productized the loop. Anthropic shipped the enforcement primitives — and its own launch copy says where the autonomy lives. In the Slay the Spire eval, persistent file-based memory improved Fable's performance three times more than for Opus 4.8 — and it reached the final act three times more often. Two distinct three-times figures, and the memory uplift is harness, not weights. Stripe reports the model performed a codebase-wide migration in a fifty-million-line Ruby codebase in a day — work that would otherwise have taken a whole team over two months by hand. Deterministic control lives in Claude Code hooks: thirty-three lifecycle events, five handler types. Exit code two is a blocking error — its standard error text feeds straight back to Claude. A stop hook exiting two prevents Claude from stopping entirely, so "when is the agent done" becomes deterministic hook code. Claude Code subagents run in their own context windows with custom system prompts and tool allowlists. Experimental agent teams claim tasks with file locking to prevent race conditions. And task budgets show the model a running token countdown for the whole agentic loop — minimum twenty thousand tokens — so the Fable 5 Claude Code loop self-moderates end to end.
