# S7 — How to Start: Your First Locked Loop | targetWords: 165 | keywords: how to run a ralph loop, autonomous coding loop tutorial, agent state files, prd.json passes true, 12-factor agents, long-running agent harness

Your first long-running agent harness is a bash wrapper, a JSON ledger full of passes false, and a completion promise — start at ten iterations, not fifty. How to run a Ralph loop: snarktank ralph, 20,118 stars — fresh instance, clean context, every iteration; default is ten; when every story hits passes true, it prints the COMPLETE promise and exits. Geocodio's weekend build is the autonomous coding loop tutorial: cat PROMPT dot md piped to claude print, max iterations fifty, agent state files — prd dot json flipping to passes true. Two full apps, Friday 4 p.m. to Sunday evening. Scope it: migrations, refactors, dependency updates, test clean-ups — definition of done stated upfront. Twelve-Factor Agents, 23,200 stars: production agents are mostly deterministic code, LLM steps sprinkled in — three to twenty steps. One practitioner's estimate: a fifty-iteration loop runs fifty to a hundred dollars plus; one dev burned two Claude Max 20x subscriptions — four hundred dollars a month — in a few days. Stay close — Ralph amplifies operator skill. Payoff: one refactor cut a test suite from four minutes to two seconds.
