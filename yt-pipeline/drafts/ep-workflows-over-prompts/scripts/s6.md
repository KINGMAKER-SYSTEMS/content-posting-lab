# S6 — The Scoreboard: Workflow Numbers vs the Best Prompt Numbers | targetWords: 170 | keywords: harness variance, agent reliability math, compounding error agents, GEPA prompt optimization, prompt sensitivity, SWE-bench scaffold

Harness variance runs 7.8 times model variance — yet the prompt side has receipts. Here's the honest scoreboard. Workflow column: agent reliability math. At 95 percent per step, a 20-step agent's odds are about 36 percent; at 99, about 82; at 100 steps, 99 falls to roughly 37. Compounding error in agents punishes the exponent; prompt tuning nudges the base. Anthropic's C compiler: 16 parallel Claude agents, just under 20,000 dollars, 100,000 lines compiling Linux 6.9. Carlini: the task verifier must be nearly perfect, or Claude solves the wrong problem. Harness-only change: Terminal-Bench 2, 69.7 to 77.0 percent on the same model — GPT-5.4, not Claude. SWE-bench scaffold variation: up to 15 points. Prompt column: OpenAI's three GPT-4.1 instructions lifted internal SWE-bench Verified close to 20 percent; planning alone, 4 percent — behavior contracts inside a harness. Prompt sensitivity to formatting swings up to 76 points on Llama 2 13B, roughly 10 across 53 tasks — older open models. GEPA prompt optimization beats GRPO by 6 percent average, up to 20, with up to 35 times fewer rollouts. Except GEPA is an automated eval loop. The workflow optimizes the prompt.
