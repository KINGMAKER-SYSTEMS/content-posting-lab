[phases.setup]
nixPkgs = ["nodejs_20", "python313", "ffmpeg", "yt-dlp", "tesseract"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "cd frontend && npm ci",
]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "python app.py"
